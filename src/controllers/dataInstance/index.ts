import get from './dataInstance.get';
import register from './dataInstance.register';
import update from './dataInstance.update';

const dataInstanceController = {
  get,
  register,
  update,
};

export default dataInstanceController;
