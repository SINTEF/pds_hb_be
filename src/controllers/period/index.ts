import register from './period.register';
import update from './period.update';
import getAll from './period.getAll';
import getOne from './period.getOne';
import del from './period.delete';

const periodController = {
  register,
  update,
  getAll,
  getOne,
  del,
};

export default periodController;
