import getOne from './notificationGroup.getOne';
import getAll from './notificationGroup.getAll';
import register from './notificationGroup.register';
import update from './notificationGroup.update';
import del from './notificationGroup.delete';

const notificationGroupController = {
  getOne,
  getAll,
  register,
  update,
  del,
};

export default notificationGroupController;
